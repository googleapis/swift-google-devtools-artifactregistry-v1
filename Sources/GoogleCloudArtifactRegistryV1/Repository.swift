// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleCloudWKT

/// A Repository for storing artifacts with a specific format.
public struct Repository: Codable, Equatable, GoogleCloudWKT._AnyPackable,
  Sendable
{
  /// The name of the repository, for example:
  /// `projects/p1/locations/us-central1/repositories/repo1`. For each location
  /// in a project, repository names must be unique.
  public var name: Swift.String = Swift.String()

  /// Optional. The format of packages that are stored in the repository.
  public var format: Repository.Format = Repository.Format()

  /// The user-provided description of the repository.
  public var description: Swift.String = Swift.String()

  /// Labels with user-defined metadata.
  /// This field may contain up to 64 entries. Label keys and values may be no
  /// longer than 63 characters. Label keys must begin with a lowercase letter
  /// and may only contain lowercase letters, numeric characters, underscores,
  /// and dashes.
  public var labels: [Swift.String: Swift.String] = [:]

  /// Output only. The time when the repository was created.
  public var createTime: GoogleCloudWKT.Timestamp? = nil

  /// Output only. The time when the repository was last updated.
  public var updateTime: GoogleCloudWKT.Timestamp? = nil

  /// The Cloud KMS resource name of the customer managed encryption key that's
  /// used to encrypt the contents of the Repository. Has the form:
  /// `projects/my-project/locations/my-region/keyRings/my-kr/cryptoKeys/my-key`.
  /// This value may not be changed after the Repository has been created.
  public var kmsKeyName: Swift.String = Swift.String()

  /// Optional. The mode of the repository.
  public var mode: Repository.Mode = Repository.Mode()

  /// Optional. Cleanup policies for this repository. Cleanup policies indicate
  /// when certain package versions can be automatically deleted. Map keys are
  /// policy IDs supplied by users during policy creation. They must unique
  /// within a repository and be under 128 characters in length.
  public var cleanupPolicies: [Swift.String: CleanupPolicy] = [:]

  /// Output only. The size, in bytes, of all artifact storage in this
  /// repository. Repositories that are generally available or in public preview
  ///  use this to calculate storage costs.
  public var sizeBytes: Swift.Int64 = Swift.Int64()

  /// Output only. Whether or not this repository satisfies PZS.
  public var satisfiesPzs: Swift.Bool = Swift.Bool()

  /// Optional. If true, the cleanup pipeline is prevented from deleting versions
  /// in this repository.
  public var cleanupPolicyDryRun: Swift.Bool = Swift.Bool()

  /// Optional. Config and state for vulnerability scanning of resources within
  /// this Repository.
  public var vulnerabilityScanningConfig: Repository.VulnerabilityScanningConfig? = nil

  /// Optional. If this is true, an unspecified repo type will be treated as
  /// error rather than defaulting to standard.
  public var disallowUnspecifiedMode: Swift.Bool = Swift.Bool()

  /// Output only. Whether or not this repository satisfies PZI.
  public var satisfiesPzi: Swift.Bool = Swift.Bool()

  /// Output only. The repository endpoint, for example:
  /// `us-docker.pkg.dev/my-proj/my-repo`.
  public var registryUri: Swift.String = Swift.String()

  /// Repository-specific configurations.
  public var formatConfig: OneOf_FormatConfig? = nil

  /// Repository configuration specific to the Mode value being selected (Remote
  /// or Virtual)
  public var modeConfig: OneOf_ModeConfig? = nil

  /// Initialize a new instance of `Repository`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = Repository().with { $0.mavenConfig = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private enum CodingKeys: Swift.String, CodingKey {
    case mavenConfig = "mavenConfig"
    case dockerConfig = "dockerConfig"
    case virtualRepositoryConfig = "virtualRepositoryConfig"
    case remoteRepositoryConfig = "remoteRepositoryConfig"
    case name = "name"
    case format = "format"
    case description = "description"
    case labels = "labels"
    case createTime = "createTime"
    case updateTime = "updateTime"
    case kmsKeyName = "kmsKeyName"
    case mode = "mode"
    case cleanupPolicies = "cleanupPolicies"
    case sizeBytes = "sizeBytes"
    case satisfiesPzs = "satisfiesPzs"
    case cleanupPolicyDryRun = "cleanupPolicyDryRun"
    case vulnerabilityScanningConfig = "vulnerabilityScanningConfig"
    case disallowUnspecifiedMode = "disallowUnspecifiedMode"
    case satisfiesPzi = "satisfiesPzi"
    case registryUri = "registryUri"
  }

  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    self.name = try container.decode(Swift.String.self, forKey: .name)
    self.format = try container.decode(Repository.Format.self, forKey: .format)
    self.description = try container.decode(Swift.String.self, forKey: .description)
    self.labels = try container.decode([Swift.String: Swift.String].self, forKey: .labels)
    self.createTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .createTime)
    self.updateTime = try container.decodeIfPresent(
      GoogleCloudWKT.Timestamp.self, forKey: .updateTime)
    self.kmsKeyName = try container.decode(Swift.String.self, forKey: .kmsKeyName)
    self.mode = try container.decode(Repository.Mode.self, forKey: .mode)
    self.cleanupPolicies = try container.decode(
      [Swift.String: CleanupPolicy].self, forKey: .cleanupPolicies)
    self.sizeBytes = try container.decode(Swift.Int64.self, forKey: .sizeBytes)
    self.satisfiesPzs = try container.decode(Swift.Bool.self, forKey: .satisfiesPzs)
    self.cleanupPolicyDryRun = try container.decode(Swift.Bool.self, forKey: .cleanupPolicyDryRun)
    self.vulnerabilityScanningConfig = try container.decodeIfPresent(
      Repository.VulnerabilityScanningConfig.self, forKey: .vulnerabilityScanningConfig)
    self.disallowUnspecifiedMode = try container.decode(
      Swift.Bool.self, forKey: .disallowUnspecifiedMode)
    self.satisfiesPzi = try container.decode(Swift.Bool.self, forKey: .satisfiesPzi)
    self.registryUri = try container.decode(Swift.String.self, forKey: .registryUri)

    var formatConfig: OneOf_FormatConfig? = nil
    let formatConfigCheckAndSet = {
      if formatConfig != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'formatConfig'"))
      }
      formatConfig = $0
    }
    if let mavenConfig = try container.decodeIfPresent(
      Repository.MavenRepositoryConfig?.self, forKey: .mavenConfig)
    {
      try formatConfigCheckAndSet(.mavenConfig(mavenConfig))
    }
    if let dockerConfig = try container.decodeIfPresent(
      Repository.DockerRepositoryConfig?.self, forKey: .dockerConfig)
    {
      try formatConfigCheckAndSet(.dockerConfig(dockerConfig))
    }
    self.formatConfig = formatConfig

    var modeConfig: OneOf_ModeConfig? = nil
    let modeConfigCheckAndSet = {
      if modeConfig != nil {
        throw DecodingError.dataCorrupted(
          DecodingError.Context(
            codingPath: decoder.codingPath,
            debugDescription: "Multiple values set for oneof 'modeConfig'"))
      }
      modeConfig = $0
    }
    if let virtualRepositoryConfig = try container.decodeIfPresent(
      VirtualRepositoryConfig?.self, forKey: .virtualRepositoryConfig)
    {
      try modeConfigCheckAndSet(.virtualRepositoryConfig(virtualRepositoryConfig))
    }
    if let remoteRepositoryConfig = try container.decodeIfPresent(
      RemoteRepositoryConfig?.self, forKey: .remoteRepositoryConfig)
    {
      try modeConfigCheckAndSet(.remoteRepositoryConfig(remoteRepositoryConfig))
    }
    self.modeConfig = modeConfig
  }

  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.name, forKey: .name)
    try container.encode(self.format, forKey: .format)
    try container.encode(self.description, forKey: .description)
    try container.encode(self.labels, forKey: .labels)
    try container.encode(self.createTime, forKey: .createTime)
    try container.encode(self.updateTime, forKey: .updateTime)
    try container.encode(self.kmsKeyName, forKey: .kmsKeyName)
    try container.encode(self.mode, forKey: .mode)
    try container.encode(self.cleanupPolicies, forKey: .cleanupPolicies)
    try container.encode(self.sizeBytes, forKey: .sizeBytes)
    try container.encode(self.satisfiesPzs, forKey: .satisfiesPzs)
    try container.encode(self.cleanupPolicyDryRun, forKey: .cleanupPolicyDryRun)
    try container.encode(self.vulnerabilityScanningConfig, forKey: .vulnerabilityScanningConfig)
    try container.encode(self.disallowUnspecifiedMode, forKey: .disallowUnspecifiedMode)
    try container.encode(self.satisfiesPzi, forKey: .satisfiesPzi)
    try container.encode(self.registryUri, forKey: .registryUri)

    if let choice = self.formatConfig {
      switch choice {
      case .mavenConfig(let value):
        try container.encode(value, forKey: .mavenConfig)
      case .dockerConfig(let value):
        try container.encode(value, forKey: .dockerConfig)
      }
    }

    if let choice = self.modeConfig {
      switch choice {
      case .virtualRepositoryConfig(let value):
        try container.encode(value, forKey: .virtualRepositoryConfig)
      case .remoteRepositoryConfig(let value):
        try container.encode(value, forKey: .remoteRepositoryConfig)
      }
    }
  }

  /// MavenRepositoryConfig is maven related repository details.
  /// Provides additional configuration details for repositories of the maven
  /// format type.
  public struct MavenRepositoryConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The repository with this flag will allow publishing
    /// the same snapshot versions.
    public var allowSnapshotOverwrites: Swift.Bool = Swift.Bool()

    /// Version policy defines the versions that the registry will accept.
    public var versionPolicy: Repository.MavenRepositoryConfig.VersionPolicy = Repository
      .MavenRepositoryConfig.VersionPolicy()

    /// Initialize a new instance of `MavenRepositoryConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = MavenRepositoryConfig().with { $0.allowSnapshotOverwrites = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// VersionPolicy is the version policy for the repository.
    public enum VersionPolicy: Codable, Equatable, Sendable {
      /// VERSION_POLICY_UNSPECIFIED - the version policy is not defined.
      /// When the version policy is not defined, no validation is performed
      /// for the versions.
      case unspecified
      /// RELEASE - repository will accept only Release versions.
      case release
      /// SNAPSHOT - repository will accept only Snapshot versions.
      case snapshot
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .release: return 1
        case .snapshot: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "VERSION_POLICY_UNSPECIFIED"
        case .release: return "RELEASE"
        case .snapshot: return "SNAPSHOT"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:VersionPolicy/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "VERSION_POLICY_UNSPECIFIED": self = .unspecified
        case "RELEASE": self = .release
        case "SNAPSHOT": self = .snapshot
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:VersionPolicy/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .release
        case 2: self = .snapshot
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .release: return try container.encode(1)
        case .snapshot: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.devtools.artifactregistry.v1.Repository.MavenRepositoryConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// DockerRepositoryConfig is docker related repository details.
  /// Provides additional configuration details for repositories of the docker
  /// format type.
  public struct DockerRepositoryConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// The repository which enabled this flag prevents all tags from being
    /// modified, moved or deleted. This does not prevent tags from being
    /// created.
    public var immutableTags: Swift.Bool = Swift.Bool()

    /// Initialize a new instance of `DockerRepositoryConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = DockerRepositoryConfig().with { $0.immutableTags = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.devtools.artifactregistry.v1.Repository.DockerRepositoryConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// Config on whether to perform vulnerability scanning for resources in this
  /// repository, as well as output fields describing current state.
  public struct VulnerabilityScanningConfig: Codable, Equatable, GoogleCloudWKT._AnyPackable,
    Sendable
  {
    /// Optional. Config for whether this repository has vulnerability scanning
    /// disabled.
    public var enablementConfig: Repository.VulnerabilityScanningConfig.EnablementConfig =
      Repository.VulnerabilityScanningConfig.EnablementConfig()

    /// Output only. The last time this repository config was enabled.
    public var lastEnableTime: GoogleCloudWKT.Timestamp? = nil

    /// Output only. State of feature enablement, combining repository enablement
    /// config and API enablement state.
    public var enablementState: Repository.VulnerabilityScanningConfig.EnablementState = Repository
      .VulnerabilityScanningConfig.EnablementState()

    /// Output only. Reason for the repository state.
    public var enablementStateReason: Swift.String = Swift.String()

    /// Initialize a new instance of `VulnerabilityScanningConfig`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = VulnerabilityScanningConfig().with { $0.enablementConfig = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    /// Config for vulnerability scanning of resources in this repository.
    public enum EnablementConfig: Codable, Equatable, Sendable {
      /// Not set. This will be treated as INHERITED for Docker repositories and
      /// DISABLED for non-Docker repositories.
      case unspecified
      /// Scanning is Enabled, but dependent on API enablement.
      case inherited
      /// No automatic vulnerability scanning will be performed for this
      /// repository.
      case disabled
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .inherited: return 1
        case .disabled: return 2
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "ENABLEMENT_CONFIG_UNSPECIFIED"
        case .inherited: return "INHERITED"
        case .disabled: return "DISABLED"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:EnablementConfig/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "ENABLEMENT_CONFIG_UNSPECIFIED": self = .unspecified
        case "INHERITED": self = .inherited
        case "DISABLED": self = .disabled
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:EnablementConfig/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .inherited
        case 2: self = .disabled
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .inherited: return try container.encode(1)
        case .disabled: return try container.encode(2)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    /// Describes the state of vulnerability scanning in this repository,
    /// including both repository enablement and API enablement.
    public enum EnablementState: Codable, Equatable, Sendable {
      /// Enablement state is unclear.
      case unspecified
      /// Repository does not support vulnerability scanning.
      case scanningUnsupported
      /// Vulnerability scanning is disabled for this repository.
      case scanningDisabled
      /// Vulnerability scanning is active for this repository.
      case scanningActive
      /// Encodes an unknown integer value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownIntValue(Int)
      /// Encodes an unknown string value.
      ///
      /// The most common cause for an unknown values is for the service to send
      /// a value unknown to the library. We recommend you update your library to
      /// the latest version.
      case unknownStringValue(String)

      public init() {
        self = .unspecified
      }

      /// Returns the integer value associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown string value, this returns `nil`.
      public var intValue: Int? {
        switch self {
        case .unspecified: return 0
        case .scanningUnsupported: return 1
        case .scanningDisabled: return 2
        case .scanningActive: return 3
        case .unknownIntValue(let v): return v
        case .unknownStringValue: return nil
        }
      }

      /// Returns the string value (or name) associated with the enumeration.
      ///
      /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
      public var stringValue: Swift.String? {
        switch self {
        case .unspecified: return "ENABLEMENT_STATE_UNSPECIFIED"
        case .scanningUnsupported: return "SCANNING_UNSUPPORTED"
        case .scanningDisabled: return "SCANNING_DISABLED"
        case .scanningActive: return "SCANNING_ACTIVE"
        case .unknownIntValue: return nil
        case .unknownStringValue(let v): return v
        }
      }

      /// Initialize from a string value.
      ///
      /// If the value is unknown, this initializes to [`unknownStringValue`](doc:EnablementState/unknownStringValue(_:)).
      public init(stringValue: Swift.String) {
        switch stringValue {
        case "ENABLEMENT_STATE_UNSPECIFIED": self = .unspecified
        case "SCANNING_UNSUPPORTED": self = .scanningUnsupported
        case "SCANNING_DISABLED": self = .scanningDisabled
        case "SCANNING_ACTIVE": self = .scanningActive
        default: self = .unknownStringValue(stringValue)
        }
      }

      /// Initialize from an integer value.
      ///
      /// If the value is unknown, this initializes to [`unknownIntValue`](doc:EnablementState/unknownIntValue(_:)).
      public init(intValue: Int) {
        switch intValue {
        case 0: self = .unspecified
        case 1: self = .scanningUnsupported
        case 2: self = .scanningDisabled
        case 3: self = .scanningActive
        default: self = .unknownIntValue(intValue)
        }
      }

      public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let v = try? container.decode(Int.self) {
          self.init(intValue: v)
          return
        }
        if let s = try? container.decode(String.self) {
          if let v = Int(s) {
            self.init(intValue: v)
          } else {
            self.init(stringValue: s)
          }
          return
        }
        throw DecodingError.dataCorruptedError(
          in: container, debugDescription: "Expected enum value, must be integer or string.")
      }

      public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .unspecified: return try container.encode(0)
        case .scanningUnsupported: return try container.encode(1)
        case .scanningDisabled: return try container.encode(2)
        case .scanningActive: return try container.encode(3)
        case .unknownIntValue(let v): return try container.encode(v)
        case .unknownStringValue(let v): return try container.encode(v)
        }
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return
        "type.googleapis.com/google.devtools.artifactregistry.v1.Repository.VulnerabilityScanningConfig"
    }
    public init(fromAny any: GoogleCloudWKT.`Any`) throws {
      self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleCloudWKT.Struct {
      return try GoogleCloudWKT._slowAnySerialize(message: self)
    }
  }

  /// A package format.
  public enum Format: Codable, Equatable, Sendable {
    /// Unspecified package format.
    case unspecified
    /// Docker package format.
    case docker
    /// Maven package format.
    case maven
    /// NPM package format.
    case npm
    /// APT package format.
    case apt
    /// YUM package format.
    case yum
    /// Python package format.
    case python
    /// Kubeflow Pipelines package format.
    case kfp
    /// Go package format.
    case go
    /// Generic package format.
    case generic
    /// Ruby package format.
    case ruby
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .docker: return 1
      case .maven: return 2
      case .npm: return 3
      case .apt: return 5
      case .yum: return 6
      case .python: return 8
      case .kfp: return 9
      case .go: return 10
      case .generic: return 11
      case .ruby: return 12
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "FORMAT_UNSPECIFIED"
      case .docker: return "DOCKER"
      case .maven: return "MAVEN"
      case .npm: return "NPM"
      case .apt: return "APT"
      case .yum: return "YUM"
      case .python: return "PYTHON"
      case .kfp: return "KFP"
      case .go: return "GO"
      case .generic: return "GENERIC"
      case .ruby: return "RUBY"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Format/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "FORMAT_UNSPECIFIED": self = .unspecified
      case "DOCKER": self = .docker
      case "MAVEN": self = .maven
      case "NPM": self = .npm
      case "APT": self = .apt
      case "YUM": self = .yum
      case "PYTHON": self = .python
      case "KFP": self = .kfp
      case "GO": self = .go
      case "GENERIC": self = .generic
      case "RUBY": self = .ruby
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Format/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .docker
      case 2: self = .maven
      case 3: self = .npm
      case 5: self = .apt
      case 6: self = .yum
      case 8: self = .python
      case 9: self = .kfp
      case 10: self = .go
      case 11: self = .generic
      case 12: self = .ruby
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .docker: return try container.encode(1)
      case .maven: return try container.encode(2)
      case .npm: return try container.encode(3)
      case .apt: return try container.encode(5)
      case .yum: return try container.encode(6)
      case .python: return try container.encode(8)
      case .kfp: return try container.encode(9)
      case .go: return try container.encode(10)
      case .generic: return try container.encode(11)
      case .ruby: return try container.encode(12)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// The mode configures the repository to serve artifacts from different
  /// sources.
  public enum Mode: Codable, Equatable, Sendable {
    /// Unspecified mode.
    case unspecified
    /// A standard repository storing artifacts.
    case standardRepository
    /// A virtual repository to serve artifacts from one or more sources.
    case virtualRepository
    /// A remote repository to serve artifacts from a remote source.
    case remoteRepository
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown values is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .standardRepository: return 1
      case .virtualRepository: return 2
      case .remoteRepository: return 3
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "MODE_UNSPECIFIED"
      case .standardRepository: return "STANDARD_REPOSITORY"
      case .virtualRepository: return "VIRTUAL_REPOSITORY"
      case .remoteRepository: return "REMOTE_REPOSITORY"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:Mode/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "MODE_UNSPECIFIED": self = .unspecified
      case "STANDARD_REPOSITORY": self = .standardRepository
      case "VIRTUAL_REPOSITORY": self = .virtualRepository
      case "REMOTE_REPOSITORY": self = .remoteRepository
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:Mode/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .standardRepository
      case 2: self = .virtualRepository
      case 3: self = .remoteRepository
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode(0)
      case .standardRepository: return try container.encode(1)
      case .virtualRepository: return try container.encode(2)
      case .remoteRepository: return try container.encode(3)
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Repository-specific configurations.
  public enum OneOf_FormatConfig: Codable, Equatable, Sendable {
    /// Maven repository config contains repository level configuration
    /// for the repositories of maven type.
    indirect case mavenConfig(Repository.MavenRepositoryConfig?)
    /// Docker repository config contains repository level configuration
    /// for the repositories of docker type.
    indirect case dockerConfig(Repository.DockerRepositoryConfig?)
  }

  /// Repository configuration specific to the Mode value being selected (Remote
  /// or Virtual)
  public enum OneOf_ModeConfig: Codable, Equatable, Sendable {
    /// Configuration specific for a Virtual Repository.
    indirect case virtualRepositoryConfig(VirtualRepositoryConfig?)
    /// Configuration specific for a Remote Repository.
    indirect case remoteRepositoryConfig(RemoteRepositoryConfig?)
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.devtools.artifactregistry.v1.Repository"
  }
  public init(fromAny any: GoogleCloudWKT.`Any`) throws {
    self = try GoogleCloudWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleCloudWKT.Struct {
    return try GoogleCloudWKT._slowAnySerialize(message: self)
  }
}
